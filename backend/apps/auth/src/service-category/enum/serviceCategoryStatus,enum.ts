export enum serviceCategoryStatus {
  ACTIVE = 'ACTIVE',
  PENDING = 'PENDING',
  PAUSED = 'PAUSED',
  BLOCKED = 'BLOCKED',
}

export enum serviceCategorycreatedBy {
  admin = 'admin',
  sericeProvider = 'sericeProvider',
}